# python3 -m cegispro2.cmd RevBin1_0.pgcl --post "z" --prop "[x=5 & z=3]*(13)"
# python3 -m cegispro2.cmd RevBin1_0.pgcl --post "z" --prop "[x=5 & z=3]*8" --invarianttype sub --cdb

nat x;
nat z;

while(0 < x){
    {x:=x-1}[0.5]{skip};
    z := z + 1
}
